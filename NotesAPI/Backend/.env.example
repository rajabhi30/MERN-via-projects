# Local MongoDB (requires MongoDB installed on your machine)
# MONGODB_URL=mongodb://127.0.0.1:27017/TASK

# MongoDB Atlas (free cloud — recommended if you don't have MongoDB locally)
MONGODB_URL=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/TASK?retryWrites=true&w=majority

PORT=5000
